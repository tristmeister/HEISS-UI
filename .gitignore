node_modules
dist
.env
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
outputs
data
*.log
*.tsbuildinfo
# Internal docs, design notes and anything else not meant for the public repo.
/private/
# Local tooling config (launch configs with machine paths).
/.claude/
.DS_Store

# Never publish product screenshots or design working files.
/product-screenshots/
/artifacts/

# PP Neue Bit is a commercial font; the app doesn't use it, keep any local copy out of the repo.
/public/fonts/PPNeueBit*
